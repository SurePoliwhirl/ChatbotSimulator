[project]
name = "chatbot-simulator-backend"
version = "0.1.0"
description = "API 키 검증 백엔드 서버"
requires-python = ">=3.8"
dependencies = [
    "flask==3.0.0",
    "flask-cors==4.0.0",
    "requests==2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

